# Outfitter Pi extension

TODO: Extract the Pi-native Outfitter extension from the CLI package into this workspace.

Today, the bootstrap extension is still generated by
`code/cli/src/cli/commands/PiLoginLaunch.ts` because it embeds launch-specific
runtime values and is covered by onboarding tests.

This directory reserves the future package boundary for reusable Pi extension
code and assets. Until that extraction is complete, this workspace is not
published independently; `private: true` in `package.json` is npm publish
protection, not repository privacy.
